# Naming

Use **kebab-case** for all file and directory names (e.g. `haptic-tab.tsx`, not `HapticTab.tsx`). Exceptions are Expo Router conventions such as `_layout.tsx` and harness entry points such as `AGENTS.md` and `CLAUDE.md`.
