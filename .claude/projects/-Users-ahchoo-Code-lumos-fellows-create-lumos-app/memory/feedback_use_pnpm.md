---
name: Use pnpm for package management
description: User prefers pnpm over npm for installing packages in this project
type: feedback
---

Always use pnpm (not npm) for installing dependencies.

**Why:** User corrected when npm was used — project convention is pnpm.

**How to apply:** Use `pnpm add`, `pnpm install`, etc. instead of `npm install`.
